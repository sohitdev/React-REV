const Loader = () => (
  <div className="flex justify-center items-center h-screen">
    <h2 className="text-white text-3xl font-bold animate-pulse">Loading...</h2>
  </div>
);

export default Loader;
